// frontend/src/components/Map/BusinessPin.tsx — placeholder for Task 9
import type { Business } from '../../types'

interface Props {
  business: Business
  isSelected: boolean
  onSelect: (id: string) => void
}

export function BusinessPin(_props: Props) {
  return null
}
